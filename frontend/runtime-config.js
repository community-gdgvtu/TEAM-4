// Safe local default. Netlify generates a deployment-specific copy in dist/.
// Values are public origins/prefixes only; never put credentials in this file.
window.SKILLPASSPORT_CONFIG = Object.freeze({
    apiBase: "",
    publicAppBase: "",
});
